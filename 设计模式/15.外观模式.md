# 外观模式

举例：

```java
class NewClass{
    psvm(String[]args){
        method1（）；
        method2（）；
        method3（）；
        method4（）；
        method5（）；
    }

   
}

class Banshouxu{
     //封装
    public void waiguan(){
        method1（）；
        method2（）；
        method3（）；
        method4（）；
        method5（）；    
    }
}

class NewClass{
    psvm(String[]args){
        BanShouxu x = new Banshouxu();
        x.waiguan();
    }
}
```
